spring:
  application:
    name: usuario-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
    compatibility-verifier:
      enabled: false

moto-service:
  url: http://moto-service

carro-service:
  url: http://carro-service